[pytest]
addopts = --strict-config --strict-markers --import-mode=importlib
testpaths = tests
markers =
    infrastructure: generic test-harness contract tests
    pki: authoritative PKI rollover tests
