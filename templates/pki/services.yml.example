services:
  platform-example:
    # Omit key_custody for managed controller-generated keys. The only
    # supported explicit value is host-local.
    # key_custody: host-local
    common_name: app.example.internal
    dns:
      - app.example.internal
      - app
    ips:
      - 192.0.2.10
    days: 397
