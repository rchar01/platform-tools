#!/usr/bin/env bash
# This script was generated by bashly 1.4.0 (https://bashly.dev)
# Modifying it manually is not recommended

if ((BASH_VERSINFO[0] < 4 || (BASH_VERSINFO[0] == 4 && BASH_VERSINFO[1] < 2))); then
  printf "bash version 4.2 or higher is required\n" >&2
  exit 1
fi

version_command() {
  echo "$version"
}

platform_pki_csr_candidate_usage() {
  printf "platform-pki-csr-candidate - Verify, finalize, or abandon authenticated CSR candidate evidence\n\n"

  printf "%s\n" "$(bold "Usage:")"
  printf "  platform-pki-csr-candidate COMMAND\n"
  printf "  platform-pki-csr-candidate [COMMAND] --help | -h\n"
  printf "  platform-pki-csr-candidate --version | -v\n"
  echo

  printf "%s\n" "$(bold "Commands:")"
  printf "  %s   Verify one exact candidate and report accepted historical state\n" "$(green "verify")  "
  printf "  %s   Accept authenticated activation and validation evidence\n" "$(green "finalize")"
  printf "  %s   Record authenticated non-activation or rollback evidence\n" "$(green "abandon") "
  echo

  if [[ -n "$long_usage" ]]; then

    printf "%s\n" "$(bold "Options:")"

    printf "  %s\n" "$(magenta "--help, -h")"
    printf "    Show this help\n"
    echo
    printf "  %s\n" "$(magenta "--version, -v")"
    printf "    Show version number\n"
    echo

    printf "Verification reports accepted historical evidence and never claims current\nlive state. Finalization verifies bounded-time authenticated evidence but\nperforms no deployment. Abandonment is not revocation. Candidate, response,\nreplay, signing transaction, export, managed keys, and managed exports remain.\n"
    echo

  fi
}

platform_pki_csr_candidate_verify_usage() {
  printf "platform-pki-csr-candidate verify - Verify one exact candidate and report accepted historical state\n\n"

  printf "%s\n" "$(bold "Usage:")"
  printf "  platform-pki-csr-candidate verify SERVICE [OPTIONS]\n"
  printf "  platform-pki-csr-candidate verify --help | -h\n"
  echo

  if [[ -n "$long_usage" ]]; then

    printf "%s\n" "$(bold "Options:")"

    printf "  %s\n" "$(magenta "--request-id ID (required)")"
    printf "    Exact 32-character lowercase hexadecimal CSR request ID\n"
    echo

    printf "  %s\n" "$(magenta "--format FORMAT")"
    printf "    Output format\n"
    printf "    %s\n" "Allowed: text, json"
    printf "    %s\n" "Default: text"
    echo

    printf "  %s\n" "$(magenta "--namespace PATH")"
    printf "    Platform namespace root\n"
    echo

    printf "  %s\n" "$(magenta "--pki-dir PATH")"
    printf "    PKI directory\n"
    echo

    printf "  %s\n" "$(magenta "--help, -h")"
    printf "    Show this help\n"
    echo

    printf "%s\n" "$(bold "Arguments:")"

    printf "  %s\n" "$(blue "SERVICE")"
    printf "    Exact inventory service\n"
    echo

  fi
}

platform_pki_csr_candidate_finalize_usage() {
  printf "platform-pki-csr-candidate finalize - Accept authenticated activation and validation evidence\n\n"

  printf "%s\n" "$(bold "Usage:")"
  printf "  platform-pki-csr-candidate finalize SERVICE [OPTIONS]\n"
  printf "  platform-pki-csr-candidate finalize --help | -h\n"
  echo

  if [[ -n "$long_usage" ]]; then

    printf "%s\n" "$(bold "Options:")"

    printf "  %s\n" "$(magenta "--request-id ID (required)")"
    printf "    Exact 32-character lowercase hexadecimal CSR request ID\n"
    echo

    printf "  %s\n" "$(magenta "--artifact-manifest-sha256 DIGEST (required)")"
    printf "    Exact lowercase SHA-256 digest of the export artifact manifest\n"
    echo

    printf "  %s\n" "$(magenta "--evidence-file PATH (required)")"
    printf "    Exact canonical deployment evidence record\n"
    echo

    printf "  %s\n" "$(magenta "--evidence-signature PATH (required)")"
    printf "    Detached OpenSSH deployment evidence signature\n"
    echo

    printf "  %s\n" "$(magenta "--yes")"
    printf "    Confirm the exact decision without a TTY\n"
    echo

    printf "  %s\n" "$(magenta "--namespace PATH")"
    printf "    Platform namespace root\n"
    echo

    printf "  %s\n" "$(magenta "--pki-dir PATH")"
    printf "    PKI directory\n"
    echo

    printf "  %s\n" "$(magenta "--help, -h")"
    printf "    Show this help\n"
    echo

    printf "%s\n" "$(bold "Arguments:")"

    printf "  %s\n" "$(blue "SERVICE")"
    printf "    Exact inventory service\n"
    echo

  fi
}

platform_pki_csr_candidate_abandon_usage() {
  printf "platform-pki-csr-candidate abandon - Record authenticated non-activation or rollback evidence\n\n"

  printf "%s\n" "$(bold "Usage:")"
  printf "  platform-pki-csr-candidate abandon SERVICE [OPTIONS]\n"
  printf "  platform-pki-csr-candidate abandon --help | -h\n"
  echo

  if [[ -n "$long_usage" ]]; then

    printf "%s\n" "$(bold "Options:")"

    printf "  %s\n" "$(magenta "--request-id ID (required)")"
    printf "    Exact 32-character lowercase hexadecimal CSR request ID\n"
    echo

    printf "  %s\n" "$(magenta "--artifact-manifest-sha256 DIGEST (required)")"
    printf "    Exact lowercase SHA-256 digest of the export artifact manifest\n"
    echo

    printf "  %s\n" "$(magenta "--evidence-file PATH (required)")"
    printf "    Exact canonical deployment evidence record\n"
    echo

    printf "  %s\n" "$(magenta "--evidence-signature PATH (required)")"
    printf "    Detached OpenSSH deployment evidence signature\n"
    echo

    printf "  %s\n" "$(magenta "--yes")"
    printf "    Confirm the exact decision without a TTY\n"
    echo

    printf "  %s\n" "$(magenta "--namespace PATH")"
    printf "    Platform namespace root\n"
    echo

    printf "  %s\n" "$(magenta "--pki-dir PATH")"
    printf "    PKI directory\n"
    echo

    printf "  %s\n" "$(magenta "--help, -h")"
    printf "    Show this help\n"
    echo

    printf "%s\n" "$(bold "Arguments:")"

    printf "  %s\n" "$(blue "SERVICE")"
    printf "    Exact inventory service\n"
    echo

  fi
}

normalize_input() {
  local arg passthru flags
  passthru=false

  while [[ $# -gt 0 ]]; do
    arg="$1"
    if [[ $passthru == true ]]; then
      input+=("$arg")
    elif [[ $arg =~ ^(--[a-zA-Z0-9_\-]+)=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^(-[a-zA-Z0-9])=(.+)$ ]]; then
      input+=("${BASH_REMATCH[1]}")
      input+=("${BASH_REMATCH[2]}")
    elif [[ $arg =~ ^-([a-zA-Z0-9][a-zA-Z0-9]+)$ ]]; then
      flags="${BASH_REMATCH[1]}"
      for ((i = 0; i < ${#flags}; i++)); do
        input+=("-${flags:i:1}")
      done
    elif [[ "$arg" == "--" ]]; then
      passthru=true
      input+=("$arg")
    else
      input+=("$arg")
    fi

    shift
  done
}

enable_auto_colors() {
  if [[ -z ${NO_COLOR+x} && ! -t 1 ]]; then NO_COLOR=1; fi
}

print_in_color() { local color=$1; shift; if [[ ${NO_COLOR:-} == '' ]]; then printf "$color%b\e[0m\n" "$*"; else printf '%b\n' "$*"; fi; }
red() { print_in_color "\e[31m" "$*"; }
green() { print_in_color "\e[32m" "$*"; }
yellow() { print_in_color "\e[33m" "$*"; }
blue() { print_in_color "\e[34m" "$*"; }
magenta() { print_in_color "\e[35m" "$*"; }
cyan() { print_in_color "\e[36m" "$*"; }
black() { print_in_color "\e[30m" "$*"; }
white() { print_in_color "\e[37m" "$*"; }
bold() { print_in_color "\e[1m" "$*"; }
underlined() { print_in_color "\e[4m" "$*"; }
bold_underlined() { print_in_color "\e[1;4m" "$*"; }
red_bold() { print_in_color "\e[1;31m" "$*"; }
green_bold() { print_in_color "\e[1;32m" "$*"; }
yellow_bold() { print_in_color "\e[1;33m" "$*"; }
blue_bold() { print_in_color "\e[1;34m" "$*"; }
magenta_bold() { print_in_color "\e[1;35m" "$*"; }
cyan_bold() { print_in_color "\e[1;36m" "$*"; }
black_bold() { print_in_color "\e[1;30m" "$*"; }
white_bold() { print_in_color "\e[1;37m" "$*"; }
red_underlined() { print_in_color "\e[4;31m" "$*"; }
green_underlined() { print_in_color "\e[4;32m" "$*"; }
yellow_underlined() { print_in_color "\e[4;33m" "$*"; }
blue_underlined() { print_in_color "\e[4;34m" "$*"; }
magenta_underlined() { print_in_color "\e[4;35m" "$*"; }
cyan_underlined() { print_in_color "\e[4;36m" "$*"; }
black_underlined() { print_in_color "\e[4;30m" "$*"; }
white_underlined() { print_in_color "\e[4;37m" "$*"; }

validate_not_empty() { [[ -n $1 ]] || printf '%s\n' 'must not be empty'; }

platform_pki_csr_candidate_verify_command() {

  pki_reject_repeated_options --request-id --format --namespace --pki-dir
  pki_candidate_run verify

}

platform_pki_csr_candidate_finalize_command() {

  pki_reject_repeated_options --request-id --artifact-manifest-sha256 --evidence-file --evidence-signature --namespace --pki-dir --yes
  candidate_confirm finalize
  pki_candidate_run finalize

}

platform_pki_csr_candidate_abandon_command() {

  pki_reject_repeated_options --request-id --artifact-manifest-sha256 --evidence-file --evidence-signature --namespace --pki-dir --yes
  candidate_confirm abandon
  pki_candidate_run abandon

}

parse_requirements() {
  local key

  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --version | -v)
        version_command
        exit
        ;;

      --help | -h)
        long_usage=yes
        platform_pki_csr_candidate_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  action=${1:-}

  case $action in
    -*) ;;

    verify)
      action="verify"
      shift
      platform_pki_csr_candidate_verify_parse_requirements "$@"
      shift $#
      ;;

    finalize)
      action="finalize"
      shift
      platform_pki_csr_candidate_finalize_parse_requirements "$@"
      shift $#
      ;;

    abandon)
      action="abandon"
      shift
      platform_pki_csr_candidate_abandon_parse_requirements "$@"
      shift $#
      ;;

    "")
      platform_pki_csr_candidate_usage >&2
      exit 1
      ;;

    *)
      printf "invalid command: %s\n" "$action" >&2
      exit 1
      ;;

  esac

  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)

        printf "invalid argument: %s\n" "$key" >&2
        exit 1

        ;;

    esac
  done

}

platform_pki_csr_candidate_verify_parse_requirements() {
  local key

  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        platform_pki_csr_candidate_verify_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  action="verify"

  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --request-id)

        if [[ -n ${2+x} ]]; then
          args['--request-id']="$2"
          shift
          shift
        else
          printf "%s\n" "--request-id requires an argument: --request-id ID" >&2
          exit 1
        fi
        ;;

      --format)

        if [[ -n ${2+x} ]]; then
          args['--format']="$2"
          shift
          shift
        else
          printf "%s\n" "--format requires an argument: --format FORMAT" >&2
          exit 1
        fi
        ;;

      --namespace)

        if [[ -n ${2+x} ]]; then
          args['--namespace']="$2"
          shift
          shift
        else
          printf "%s\n" "--namespace requires an argument: --namespace PATH" >&2
          exit 1
        fi
        ;;

      --pki-dir)

        if [[ -n ${2+x} ]]; then
          args['--pki-dir']="$2"
          shift
          shift
        else
          printf "%s\n" "--pki-dir requires an argument: --pki-dir PATH" >&2
          exit 1
        fi
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)

        if [[ -z ${args['service']+x} ]]; then
          args['service']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

  if [[ -z ${args['service']+x} ]]; then
    printf "missing required argument: SERVICE\nusage: platform-pki-csr-candidate verify SERVICE [OPTIONS]\n" >&2

    exit 1
  fi

  if [[ -z ${args['--request-id']+x} ]]; then
    printf "missing required flag: --request-id ID\n" >&2
    exit 1
  fi

  [[ -n ${args['--format']:-} ]] || args['--format']="text"

  if [[ -v args['--request-id'] ]]; then
    validation_output="$(validate_not_empty "${args['--request-id']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--request-id ID" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--namespace'] ]]; then
    validation_output="$(validate_not_empty "${args['--namespace']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--namespace PATH" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--pki-dir'] ]]; then
    validation_output="$(validate_not_empty "${args['--pki-dir']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--pki-dir PATH" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ ${args['--format']:-} ]] && [[ ! ${args['--format']:-} =~ ^(text|json)$ ]]; then
    printf "%s\n" "--format must be one of: text, json" >&2
    exit 1
  fi

}

platform_pki_csr_candidate_finalize_parse_requirements() {
  local key

  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        platform_pki_csr_candidate_finalize_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  action="finalize"

  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --request-id)

        if [[ -n ${2+x} ]]; then
          args['--request-id']="$2"
          shift
          shift
        else
          printf "%s\n" "--request-id requires an argument: --request-id ID" >&2
          exit 1
        fi
        ;;

      --artifact-manifest-sha256)

        if [[ -n ${2+x} ]]; then
          args['--artifact-manifest-sha256']="$2"
          shift
          shift
        else
          printf "%s\n" "--artifact-manifest-sha256 requires an argument: --artifact-manifest-sha256 DIGEST" >&2
          exit 1
        fi
        ;;

      --evidence-file)

        if [[ -n ${2+x} ]]; then
          args['--evidence-file']="$2"
          shift
          shift
        else
          printf "%s\n" "--evidence-file requires an argument: --evidence-file PATH" >&2
          exit 1
        fi
        ;;

      --evidence-signature)

        if [[ -n ${2+x} ]]; then
          args['--evidence-signature']="$2"
          shift
          shift
        else
          printf "%s\n" "--evidence-signature requires an argument: --evidence-signature PATH" >&2
          exit 1
        fi
        ;;

      --yes)

        args['--yes']=1
        shift
        ;;

      --namespace)

        if [[ -n ${2+x} ]]; then
          args['--namespace']="$2"
          shift
          shift
        else
          printf "%s\n" "--namespace requires an argument: --namespace PATH" >&2
          exit 1
        fi
        ;;

      --pki-dir)

        if [[ -n ${2+x} ]]; then
          args['--pki-dir']="$2"
          shift
          shift
        else
          printf "%s\n" "--pki-dir requires an argument: --pki-dir PATH" >&2
          exit 1
        fi
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)

        if [[ -z ${args['service']+x} ]]; then
          args['service']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

  if [[ -z ${args['service']+x} ]]; then
    printf "missing required argument: SERVICE\nusage: platform-pki-csr-candidate finalize SERVICE [OPTIONS]\n" >&2

    exit 1
  fi

  if [[ -z ${args['--request-id']+x} ]]; then
    printf "missing required flag: --request-id ID\n" >&2
    exit 1
  fi
  if [[ -z ${args['--artifact-manifest-sha256']+x} ]]; then
    printf "missing required flag: --artifact-manifest-sha256 DIGEST\n" >&2
    exit 1
  fi
  if [[ -z ${args['--evidence-file']+x} ]]; then
    printf "missing required flag: --evidence-file PATH\n" >&2
    exit 1
  fi
  if [[ -z ${args['--evidence-signature']+x} ]]; then
    printf "missing required flag: --evidence-signature PATH\n" >&2
    exit 1
  fi

  if [[ -v args['--request-id'] ]]; then
    validation_output="$(validate_not_empty "${args['--request-id']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--request-id ID" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--artifact-manifest-sha256'] ]]; then
    validation_output="$(validate_not_empty "${args['--artifact-manifest-sha256']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--artifact-manifest-sha256 DIGEST" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--evidence-file'] ]]; then
    validation_output="$(validate_not_empty "${args['--evidence-file']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--evidence-file PATH" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--evidence-signature'] ]]; then
    validation_output="$(validate_not_empty "${args['--evidence-signature']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--evidence-signature PATH" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--namespace'] ]]; then
    validation_output="$(validate_not_empty "${args['--namespace']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--namespace PATH" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--pki-dir'] ]]; then
    validation_output="$(validate_not_empty "${args['--pki-dir']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--pki-dir PATH" "$validation_output" >&2
      exit 1
    fi
  fi

}

platform_pki_csr_candidate_abandon_parse_requirements() {
  local key

  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in
      --help | -h)
        long_usage=yes
        platform_pki_csr_candidate_abandon_usage
        exit
        ;;

      *)
        break
        ;;

    esac
  done

  action="abandon"

  while [[ $# -gt 0 ]]; do
    key="$1"
    case "$key" in

      --request-id)

        if [[ -n ${2+x} ]]; then
          args['--request-id']="$2"
          shift
          shift
        else
          printf "%s\n" "--request-id requires an argument: --request-id ID" >&2
          exit 1
        fi
        ;;

      --artifact-manifest-sha256)

        if [[ -n ${2+x} ]]; then
          args['--artifact-manifest-sha256']="$2"
          shift
          shift
        else
          printf "%s\n" "--artifact-manifest-sha256 requires an argument: --artifact-manifest-sha256 DIGEST" >&2
          exit 1
        fi
        ;;

      --evidence-file)

        if [[ -n ${2+x} ]]; then
          args['--evidence-file']="$2"
          shift
          shift
        else
          printf "%s\n" "--evidence-file requires an argument: --evidence-file PATH" >&2
          exit 1
        fi
        ;;

      --evidence-signature)

        if [[ -n ${2+x} ]]; then
          args['--evidence-signature']="$2"
          shift
          shift
        else
          printf "%s\n" "--evidence-signature requires an argument: --evidence-signature PATH" >&2
          exit 1
        fi
        ;;

      --yes)

        args['--yes']=1
        shift
        ;;

      --namespace)

        if [[ -n ${2+x} ]]; then
          args['--namespace']="$2"
          shift
          shift
        else
          printf "%s\n" "--namespace requires an argument: --namespace PATH" >&2
          exit 1
        fi
        ;;

      --pki-dir)

        if [[ -n ${2+x} ]]; then
          args['--pki-dir']="$2"
          shift
          shift
        else
          printf "%s\n" "--pki-dir requires an argument: --pki-dir PATH" >&2
          exit 1
        fi
        ;;

      -?*)
        printf "invalid option: %s\n" "$key" >&2
        exit 1
        ;;

      *)

        if [[ -z ${args['service']+x} ]]; then
          args['service']=$1
          shift
        else
          printf "invalid argument: %s\n" "$key" >&2
          exit 1
        fi

        ;;

    esac
  done

  if [[ -z ${args['service']+x} ]]; then
    printf "missing required argument: SERVICE\nusage: platform-pki-csr-candidate abandon SERVICE [OPTIONS]\n" >&2

    exit 1
  fi

  if [[ -z ${args['--request-id']+x} ]]; then
    printf "missing required flag: --request-id ID\n" >&2
    exit 1
  fi
  if [[ -z ${args['--artifact-manifest-sha256']+x} ]]; then
    printf "missing required flag: --artifact-manifest-sha256 DIGEST\n" >&2
    exit 1
  fi
  if [[ -z ${args['--evidence-file']+x} ]]; then
    printf "missing required flag: --evidence-file PATH\n" >&2
    exit 1
  fi
  if [[ -z ${args['--evidence-signature']+x} ]]; then
    printf "missing required flag: --evidence-signature PATH\n" >&2
    exit 1
  fi

  if [[ -v args['--request-id'] ]]; then
    validation_output="$(validate_not_empty "${args['--request-id']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--request-id ID" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--artifact-manifest-sha256'] ]]; then
    validation_output="$(validate_not_empty "${args['--artifact-manifest-sha256']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--artifact-manifest-sha256 DIGEST" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--evidence-file'] ]]; then
    validation_output="$(validate_not_empty "${args['--evidence-file']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--evidence-file PATH" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--evidence-signature'] ]]; then
    validation_output="$(validate_not_empty "${args['--evidence-signature']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--evidence-signature PATH" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--namespace'] ]]; then
    validation_output="$(validate_not_empty "${args['--namespace']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--namespace PATH" "$validation_output" >&2
      exit 1
    fi
  fi

  if [[ -v args['--pki-dir'] ]]; then
    validation_output="$(validate_not_empty "${args['--pki-dir']:-}")"
    if [[ -n "${validation_output}" ]]; then
      printf "validation error in %s:\n%s\n" "--pki-dir PATH" "$validation_output" >&2
      exit 1
    fi
  fi

}

initialize() {
  declare -g version="platform-pki-csr-candidate 2.2.0"
  set -euo pipefail

  enable_auto_colors

  SCRIPT_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)
  COMMON_PATH=${PLATFORM_TOOLS_LIB_DIR:-}
  if [[ -n $COMMON_PATH ]]; then
    COMMON_PATH=${COMMON_PATH}/platform-pki-common.sh
  elif [[ -r ${SCRIPT_DIR}/../lib/platform-pki-common.sh ]]; then
    COMMON_PATH=${SCRIPT_DIR}/../lib/platform-pki-common.sh
  else
    COMMON_PATH=${PLATFORM_TOOLS_SHARE_DIR:-${XDG_DATA_HOME:-$HOME/.local/share}/platform-tools}/lib/platform-pki-common.sh
  fi
  [[ -r $COMMON_PATH ]] || { printf '[ERROR] platform-pki-common.sh not found\n' >&2; exit 1; }
  # shellcheck source=../../../../lib/platform-pki-common.sh disable=SC1091
  source "$COMMON_PATH"
  CSR_COMMON_PATH=$(dirname -- "$COMMON_PATH")/platform-pki-csr-sign.sh
  CANDIDATE_COMMON_PATH=$(dirname -- "$COMMON_PATH")/platform-pki-csr-candidate.sh
  [[ -r $CSR_COMMON_PATH && -r $CANDIDATE_COMMON_PATH ]] || { printf '[ERROR] PKI CSR shared libraries not found\n' >&2; exit 1; }
  # shellcheck source=../../../../lib/platform-pki-csr-sign.sh disable=SC1091
  source "$CSR_COMMON_PATH"
  # shellcheck source=../../../../lib/platform-pki-csr-candidate.sh disable=SC1091
  source "$CANDIDATE_COMMON_PATH"

  candidate_confirm() {
    local action=$1 service=${args[service]} request_id=${args[--request-id]}
    [[ -v args[--yes] ]] && return 0
    [[ -t 0 ]] || pki_die "CSR candidate $action requires a TTY or --yes"
    printf 'Type %s %s %s to continue: ' "$action" "$service" "$request_id" >&2
    IFS= read -r confirmation
    [[ $confirmation == "$action $service $request_id" ]] || pki_die "CSR candidate $action confirmation did not match"
  }

}

run() {

  declare -g long_usage=''
  declare -g -A args=()
  declare -g -a input=()

  normalize_input "$@"
  parse_requirements "${input[@]}"

  case "$action" in
    "verify") platform_pki_csr_candidate_verify_command ;;
    "finalize") platform_pki_csr_candidate_finalize_command ;;
    "abandon") platform_pki_csr_candidate_abandon_command ;;
  esac
}

command_line_args=("$@")
initialize
run "${command_line_args[@]}"
